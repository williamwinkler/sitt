// use rocket::{post, State};
// use crate::User;

// #[post("/track/<project_id>/start")]
// pub async fn start(track_service: &State<TrackService>, user: &State<User>, project_id: &str) {
//   let created_by = &user.name;
//   match track_service.start(project_id, created_by) {

//   }
// }
